var assert = require('assert-diff');

it('choose youtube as provider based on the URL', function (done) {
	assert.fail('implement test');
});

it('choose soundcloud as provider based on the URL', function (done) {
	assert.fail('implement test');
});

it('choose mixcloud as provider based on the URL', function (done) {
	assert.fail('implement test');
});

it('choose if mode is song or playlist based on the URL', function (done) {
	assert.fail('implement test');
});

it('generate soundcloud playlist text file', function (done) {
	assert.fail('implement test');

});

it('generate youtube playlist text file', function (done) {
	assert.fail('implement test');

});

it('generate mixcloud playlist text file', function (done) {
	assert.fail('implement test');
});
